<h1> All Dogs </h1>

<div class="card-columns">
  <% @posts.each do |post| %>
  <div class="card">
  <%= link_to(image_tag(post.image, class:"card-img", width:"100%"), post) %>
  </div>
  <% end %>
</div>
